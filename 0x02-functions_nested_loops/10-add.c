#include "main.h"
/**
 * add - add @a and @b
 * @a: first parameter for the addition
 * @b: second parameter for the addition
 * Return: result of addition
 */
int add(int a, int b)
{
	return (a + b);
}
