#include <stdio.h>
/**
 * main - print all single digit of base 10 starting for 0
 *
 * Return: 0, if successful, otherwise non-zero
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d",i);
	printf("\n");
	return (0);
}
