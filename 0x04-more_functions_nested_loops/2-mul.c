#include "main.h"
/**
 * mul - multiply two integers
 * @a: first argument up for multiplication
 * @b: second argument up for multiplication
 * Return: multiple of the two integers
 */

int mul(int a, int b)
{
	return (a * b);
}
